# Sailboat Mod 开发任务书

## 当前进度总览

**最后更新**: 2026-03-31

### 已完成的核心功能

#### 1. 居民系统 (ResidentEntity)
- ✅ 基础数据模型 `ResidentRecord`
  - 字段: residentId, townId, name, skinHash, profession, gender, age, hunger, educated, culture, level, happiness
  - 最大饥饿值: 100
  - 教育加成: 1.5x 生产效率
- ✅ 实体类 `ResidentEntity`
  - 继承 PathfinderMob
  - 同步数据字段 (EntityDataAccessor)
  - NBT 持久化
  - 右键交互显示信息 (名字/性别/年龄/职业/饥饿/教育/文化)
- ✅ 性别系统 `Gender` (MALE/FEMALE)
- ✅ 文化系统 `Culture` (EUROPEAN/ASIAN/NORDIC/DESERT/TROPICAL/SLAVIC)
- ✅ 职业系统 `Profession`
  - 新增 UNEMPLOYED (无业游民) 作为默认职业
  - 其他职业: FARMER, MINER, LUMBERJACK, FISHERMAN, BLACKSMITH, BAKER, GUARD, SOLDIER

#### 2. 建筑系统
- ✅ 小屋核心 `CottageBlock` (3个变体)
  - cottage_small.nbt (200方块, 容量2人)
  - cottage_medium.nbt (328方块, 容量4人)
  - cottage_large.nbt (488方块, 容量6人)
  - 全息名称显示 (CottageBlockEntityRenderer)
- ✅ 酒馆核心 `BarBlock`
  - tavern.nbt (623方块)
  - 招募居民功能 (消耗 3面包 + 1绿宝石)
  - 检查住房容量 (32格范围内小屋)
  - 招募的居民初始为 UNEMPLOYED
  - 全息名称显示 (BarBlockEntityRenderer)
- ✅ 兵营 `BarracksBlock`
  - 征召平民成为士兵
  - 从32格范围内找到平民居民实体
  - 移除平民实体，生成士兵实体
  - 编入军队名单
- ✅ 银行 `BankBlock` (已有全息名称显示)
- ✅ 市场 `MarketBlock` (已添加全息名称显示)
- ✅ 码头 `DockBlock` (已添加全息名称显示)

#### 3. 军队系统
- ✅ 士兵实体 `SoldierEntity`
  - 继承 ResidentEntity
  - 战斗属性: 30HP, 4攻击, 2护甲
  - armyId 字段关联军队
- ✅ 军队数据模型 `ArmyRecord`
  - 字段: armyId, nationId, name, commanderUuid, formation, stance, state, soldierIds, rallyPoint, targetPoint
  - 最大士兵数: 20
- ✅ 阵型系统 `Formation`
  - LINE (横排), COLUMN (纵队), WEDGE (楔形), SQUARE (方阵), SCATTER (散开)
  - 每个阵型有 getSlotOffset() 计算士兵位置
- ✅ 姿态系统 `Stance`
  - AGGRESSIVE (主动), DEFENSIVE (防御), HOLD (固守), PATROL (巡逻)
- ✅ 状态机 `ArmyState`
  - IDLE, RALLYING, MARCHING, ATTACKING, DEFENDING, RETREATING
- ✅ AI 目标系统
  - `FollowCommandGoal` (最高优先级, 执行命令)
  - `FormationKeepGoal` (保持阵型)
  - `ThreatScanGoal` (扫描威胁)
- ✅ 指挥杖 `CommandBatonItem`
  - 右键地面: 行军
  - Shift+右键地面: 攻击移动
  - 右键实体: 攻击目标
  - 右键空气: 集结

#### 4. 城镇文化系统
- ✅ TownRecord 添加 cultureId 字段
- ⚠️ **部分完成**: TownService.java 中大部分构造函数调用已修复
  - 已用 sed 批量修复 10 处
  - **待修复**: 第 307 行和第 549 行 (新建城镇时的构造函数)

#### 5. 皮肤生成系统
- ✅ 工具脚本 `tools/gen_skins.py`
  - AI 模式: FLUX.2-klein-9B + MCSkin LoRA (已安装)
  - 程序化模式: 按职业生成简单彩色皮肤
  - 已生成 80 张程序化皮肤 (每职业10张)
- ⏳ **待完成**: ComfyUI workflow JSON 配置
- ⏳ **待完成**: 批量生成脚本 (调用 ComfyUI API)

#### 6. 语言文件
- ✅ 中文翻译 `zh_cn.json`
  - 居民信息显示: `entity.sailboatmod.resident.info`
  - 酒馆招募消息
  - 兵营征召消息
  - 小屋/酒馆/兵营方块名称
- ✅ 英文翻译 `en_us.json` (同步更新)

---

## 🔴 当前紧急任务

### 1. 修复 TownRecord 构造函数调用 (编译阻塞)

**文件**: `TownService.java`

**问题**: 添加 cultureId 字段后，构造函数从 8 参数变为 9 参数，有 2 处未修复

**待修复位置**:

#### 第 549 行 (新建城镇)
```java
// 当前代码 (错误):
TownRecord town = new TownRecord(
    UUID.randomUUID().toString().replace("-", ""),
    nationId, name, actor.getUUID(), now,
    "", TownRecord.noCorePos(), ""
);

// 修复为:
TownRecord town = new TownRecord(
    UUID.randomUUID().toString().replace("-", ""),
    nationId, name, actor.getUUID(), now,
    "", TownRecord.noCorePos(), "", "european"  // 添加默认文化
);
```

#### 第 307 行 (移除城镇核心)
```java
// 当前代码 (错误):
TownRecord updated = new TownRecord(
    town.townId(), town.nationId(), town.name(),
    town.mayorUuid(), town.createdAt(),
    "", TownRecord.noCorePos(), town.flagId()
);

// 修复为:
TownRecord updated = new TownRecord(
    town.townId(), town.nationId(), town.name(),
    town.mayorUuid(), town.createdAt(),
    "", TownRecord.noCorePos(), town.flagId(), town.cultureId()  // 保留原文化
);
```

**验证步骤**:
```bash
cd F:\Codex\sailboatmod
./gradlew compileJava
```

---

## 📋 下一步开发任务 (优先级排序)

### 优先级 1: 完成皮肤生成系统

#### 任务 1.1: 编写 ComfyUI Workflow JSON
**文件**: `tools/comfyui_workflow_mcskin.json`

**需要的节点**:
1. CheckpointLoaderSimple (加载 FLUX.2-klein-9B)
2. LoraLoader (加载 MCSkin LoRA)
3. CLIPTextEncode (正向提示词)
4. CLIPTextEncode (负向提示词)
5. KSampler (采样器)
6. VAEDecode (解码)
7. SaveImage (保存)

**提示词模板**:
```
正向: "minecraft skin, {profession}, {gender}, {culture} style, 64x64 pixel art, front view, T-pose"
负向: "blurry, low quality, distorted, 3d render"
```

#### 任务 1.2: 编写批量生成脚本
**文件**: `tools/batch_gen_skins.py`

**功能**:
- 读取 ComfyUI workflow JSON
- 遍历所有职业 × 性别 × 文化组合
- 调用 ComfyUI API (http://127.0.0.1:8188/prompt)
- 保存到 `pixel/skins/{profession}_{gender}_{culture}_{index}.png`
- 生成目标: 每组合 5 张，共 8职业 × 2性别 × 6文化 × 5张 = 480 张

**命令**:
```bash
cd F:\Codex\sailboatmod\tools
python batch_gen_skins.py --count 5 --output ../pixel/skins
```

---

### 优先级 2: 城镇经济统计页面

#### 任务 2.1: 添加 Town 经济数据模型
**文件**: `TownEconomyRecord.java` (新建)

**字段**:
```java
public record TownEconomyRecord(
    String townId,
    int totalPopulation,      // 总人口
    int employedCount,        // 就业人数
    int unemployedCount,      // 失业人数
    int literateCount,        // 识字人数
    float unemploymentRate,   // 失业率 (%)
    float literacyRate,       // 识字率 (%)
    long lastUpdated
) {
    public static TownEconomyRecord calculate(TownRecord town, List<ResidentRecord> residents) {
        int total = residents.size();
        int employed = (int) residents.stream().filter(r -> r.profession() != Profession.UNEMPLOYED).count();
        int literate = (int) residents.stream().filter(ResidentRecord::educated).count();
        return new TownEconomyRecord(
            town.townId(), total, employed, total - employed, literate,
            total > 0 ? (float)(total - employed) / total * 100 : 0,
            total > 0 ? (float)literate / total * 100 : 0,
            System.currentTimeMillis()
        );
    }
}
```

#### 任务 2.2: 添加 Town 菜单经济页面
**文件**: `TownScreen.java`

**新增标签页**: "经济" (Economy)

**显示内容**:
```
总人口: 45
就业人数: 32 (71.1%)
失业人数: 13 (28.9%)
识字人数: 18 (40.0%)

职业分布:
  农民: 8
  矿工: 6
  伐木工: 5
  渔夫: 4
  铁匠: 3
  面包师: 3
  守卫: 3
  无业游民: 13
```

**参考**: Victoria 3 的人口统计界面

---

### 优先级 3: 国家经济统计 (Bank 界面)

#### 任务 3.1: 添加 Nation 经济数据模型
**文件**: `NationEconomyRecord.java` (新建)

**字段**:
```java
public record NationEconomyRecord(
    String nationId,
    int totalPopulation,
    int totalTowns,
    float avgUnemploymentRate,
    float avgLiteracyRate,
    long totalTreasuryBalance,
    long dailyTaxIncome,
    long dailyExpenditure,
    Map<String, Integer> productionByType,  // 各类物品日产量
    long lastUpdated
) {}
```

#### 任务 3.2: Bank 界面添加统计页面
**文件**: `BankScreen.java`

**新增标签页**: "国家经济" (National Economy)

**显示内容**:
```
国家总人口: 234
城镇数量: 5
平均失业率: 22.3%
平均识字率: 45.6%

国库余额: 12,450
日税收: +850
日支出: -320
净收入: +530

日产量统计:
  小麦: 120
  铁锭: 45
  木板: 89
  面包: 67
```

---

### 优先级 4: 居民求职系统

#### 任务 4.1: 工作站方块
**文件**: `WorkstationBlock.java` (新建)

**类型**:
- FarmWorkstation (农田)
- MineWorkstation (矿场)
- LumberyardWorkstation (伐木场)
- FisheryWorkstation (渔场)
- ForgeWorkstation (铁匠铺)
- BakeryWorkstation (面包房)

**功能**:
- 放置后自动注册到城镇
- 有职位容量 (1-4个)
- UNEMPLOYED 居民会自动寻找最近的空闲工作站

#### 任务 4.2: 求职 AI Goal
**文件**: `FindJobGoal.java` (新建)

**逻辑**:
1. 检查自己是否 UNEMPLOYED
2. 扫描 64 格范围内的工作站
3. 找到有空位的工作站
4. 导航过去并"上班"
5. 更新 profession 和 assignedWorkplace

---

### 优先级 5: 教育系统

#### 任务 5.1: 学校方块
**文件**: `SchoolBlock.java` (新建)

**功能**:
- 容纳 10 个学生
- 未受教育的居民会自动入学
- 学习 7 天后 educated = true

#### 任务 5.2: 学习 AI Goal
**文件**: `StudyGoal.java` (新建)

**逻辑**:
1. 检查自己是否未受教育
2. 找到最近的学校
3. 每天去学校"上课"
4. 7 天后毕业

---

## 🗂️ 文件结构总览

```
sailboatmod/
├── src/main/java/com/monpai/sailboatmod/
│   ├── resident/
│   │   ├── entity/
│   │   │   ├── ResidentEntity.java          ✅
│   │   │   └── SoldierEntity.java           ✅
│   │   ├── model/
│   │   │   ├── ResidentRecord.java          ✅
│   │   │   ├── Gender.java                  ✅
│   │   │   ├── Culture.java                 ✅
│   │   │   └── Profession.java              ✅
│   │   ├── ai/
│   │   │   ├── FindJobGoal.java             ⏳
│   │   │   └── StudyGoal.java               ⏳
│   │   └── block/
│   │       ├── CottageBlock.java            ✅
│   │       ├── BarBlock.java                ✅
│   │       ├── WorkstationBlock.java        ⏳
│   │       └── SchoolBlock.java             ⏳
│   ├── army/
│   │   ├── entity/SoldierEntity.java        ✅
│   │   ├── model/
│   │   │   ├── ArmyRecord.java              ✅
│   │   │   ├── Formation.java               ✅
│   │   │   ├── Stance.java                  ✅
│   │   │   └── ArmyState.java               ✅
│   │   ├── ai/
│   │   │   ├── FollowCommandGoal.java       ✅
│   │   │   ├── FormationKeepGoal.java       ✅
│   │   │   └── ThreatScanGoal.java          ✅
│   │   ├── block/BarracksBlock.java         ✅
│   │   └── item/CommandBatonItem.java       ✅
│   ├── nation/
│   │   ├── model/
│   │   │   ├── TownRecord.java              ⚠️ (2处待修复)
│   │   │   ├── TownEconomyRecord.java       ⏳
│   │   │   └── NationEconomyRecord.java     ⏳
│   │   └── service/
│   │       └── TownService.java             ⚠️ (2处待修复)
│   └── bank/
│       ├── block/BankBlock.java             ✅
│       └── screen/BankScreen.java           ⏳ (待添加统计页)
├── src/main/resources/
│   ├── assets/sailboatmod/
│   │   ├── lang/
│   │   │   ├── zh_cn.json                   ✅
│   │   │   └── en_us.json                   ✅
│   │   └── textures/
│   │       └── block/
│   │           ├── cottage_core.png         ✅
│   │           └── bar_core.png             ✅
│   └── data/sailboatmod/
│       └── structures/
│           ├── cottage_small.nbt            ✅
│           ├── cottage_medium.nbt           ✅
│           ├── cottage_large.nbt            ✅
│           └── tavern.nbt                   ✅
├── tools/
│   ├── gen_structures.py                    ✅
│   ├── gen_skins.py                         ✅
│   ├── comfyui_workflow_mcskin.json         ⏳
│   └── batch_gen_skins.py                   ⏳
└── pixel/
    └── skins/                                ⏳ (待生成480张)
```

---

## 🐛 已知问题

1. **编译错误**: TownService.java 第 307, 549 行缺少 cultureId 参数
2. **皮肤生成**: ComfyUI workflow 尚未配置
3. **经济统计**: Town 和 Nation 层面的统计界面未实现
4. **求职系统**: UNEMPLOYED 居民无法自动找工作
5. **教育系统**: 没有学校和学习机制

---

## 📝 开发笔记

### 设计理念
- **维多利亚3风格**: 人口统计、失业率、识字率、生产链
- **骑砍/Holdfast风格**: 军队集结→行军→战斗，阵型系统
- **资源驱动**: 招募需要物品而非经验值
- **文化多样性**: 不同文化影响名字和外观

### 技术要点
- **数据持久化**: 使用 SavedData 模式存储 TownRecord, ResidentRecord, ArmyRecord
- **客户端同步**: 使用 SynchedEntityData 同步实体字段
- **AI 系统**: 使用 Goal 优先级系统控制 NPC 行为
- **阵型数学**: 每个阵型有独立的 slot offset 计算公式

### 性能优化
- 皮肤预生成而非实时生成
- 工作站扫描限制在 64 格范围
- 军队寻路使用共享路径 + 阵型偏移

---

## 🚀 快速启动命令

```bash
# 编译检查
cd F:\Codex\sailboatmod
./gradlew compileJava

# 运行游戏测试
./gradlew runClient

# 生成皮肤 (ComfyUI 需先启动)
cd tools
python batch_gen_skins.py --count 5

# 生成结构 NBT
python gen_structures.py
```

---

## 📚 参考资料

- **Victoria 3**: 人口系统、经济统计
- **Mount & Blade**: 军队指挥、阵型系统
- **Holdfast**: 集结机制、RTS 风格
- **ComfyUI**: AI 图像生成工作流
- **FLUX.2-klein-9B**: 扩散模型
- **MCSkin LoRA**: Minecraft 皮肤风格适配器

---

**下次打开时优先做**:
1. 修复 TownService.java 第 307, 549 行
2. 运行 `./gradlew compileJava` 验证
3. 编写 ComfyUI workflow JSON
4. 批量生成 480 张 NPC 皮肤
