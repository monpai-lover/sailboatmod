package com.monpai.sailboatmod.roadplanner.model;

public enum RoadToolType {
    ROAD,
    BRIDGE,
    TUNNEL,
    ERASE,
    SELECT,
    WATER_CROSSING,
    BEZIER,
    ENDPOINT,
    FORCE_RENDER
}
