## 更新内容

本版是 1.3.9 之后的**稳定版**，集中修复多 mod 服务器环境下暴露的运输载具可见性、聊天与界面交互问题（均经实机多轮验证）。

- 幽灵马车修复（核心，实测确认）
  - 修复多 mod 服务器上自动驾驶马车返航时「只听到音效、看不见车、实体判定箱也框不到」的幽灵车问题。
  - 根因：某些 mod 干扰了原版 EntityTracker，载具被强制加载的区块里 tick 但客户端从未收到生成包。
  - 修法：载具自动驾驶 + 到站宽限期内，对刚进入视野范围（约 96 格）的玩家补发一次实体生成包，之后持续补发位置/速度同步包替代失效的 EntityTracker，使返航途中马车一进入视野即可见并平滑跟随移动。
  - 到站「已到站」悬浮提示永不消失的问题一并修复（改为服务端权威倒计时，不再依赖客户端 tick）。

- 聊天前缀重复
  - 修复带国家的玩家发言时，聊天显示成 `<[国][职]名> [国][职]内容` 的前缀重复。
  - 前缀只加在玩家显示名上，不再额外拼进消息正文。

- 驿站派遣「可达城镇」列表稳定（承接 1.3.9 后续修复）
  - 修复未走近目的地、目标区块未加载时可达城镇选项间歇消失、需走近才出现的问题。
  - 可达性判定只依赖持久化路网，不再受区块是否加载影响。

- 网页地图轨迹信息完善
  - 手动发运的物流轨迹补充当前速度、ETA、货舱货品显示，命名改为 town→town 形式。

- 清理
  - 移除马车自动驾驶的调试日志，避免刷屏。

## 已知问题

- 商店改名等输入框暂不支持直接切输入法打中文（IME）。本想在本版加入，但实现方式在生产环境会导致客户端崩溃（见下条），已回退；暂时可用**复制粘贴**把中文填入输入框。后续版本会用安全方式重新支持。
- 部分整合包在 Temurin/OpenJDK 17.0.19 下可能出现 JVM 致命崩溃（`EXCEPTION_ACCESS_VIOLATION`，崩溃栈在 JIT 编译线程或已编译的区块/调色板解包代码）。经崩溃日志确认为该 JVM 版本的 C2 即时编译器 bug，与本 mod 逻辑无关，重启即恢复。建议升级 JVM 补丁版本或在启动参数中绕过对应编译热点。

## 修订

- **发布当天热修**：1.3.10 初版为支持中文输入法在市场界面覆写了一个被上游库声明为 final 的方法，导致客户端在加载市场界面类时抛 `IncompatibleClassChangeError` 崩溃、无法进入世界。已移除该覆写并**原地替换本 release 的 jar**。若你在 14:51(UTC) 之前下载过 1.3.10，请重新下载。

## 验证

- Gradle build: BUILD SUCCESSFUL
- Java 字节码: Java 17
- 幽灵马车返航可见性：实机多 mod 服务器验证通过

## 文件

- sailboatmod-1.3.10-all.jar（主 mod）
- sailboatmod-marketweb-1.3.10.jar（网页地图前端）
