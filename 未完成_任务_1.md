# 未完成任务 1

## 说明

当前 `main` 已完成并发布的重点是道路规划器、贝塞尔道路 ribbon、道路生命周期、驿站道路锚点辅助与相关稳定性修复。  
本文件只记录截至 `2026-04-10` 仍未完成、未闭环，或者仍停留在本地备份中的内容。

当前还有一份未恢复的备份：

- `git stash show --stat "stash@{0}"`
- 说明：`backup-before-merge-feature-bezier-road-ribbon-into-main-2026-04-10`

这份 stash 主要包含马车系统、界面显示与部分道路预览相关的在制修改，尚未正式并入 `main`。

## 一、马车系统收尾

- 完成马车木材材质方案落地，不再继续使用单一旧贴图。
  - 目标：按木材类型驱动模型贴图或着色。
  - 相关计划文件：`docs/superpowers/plans/2026-04-09-carriage-road-total-fixes.md`
  - 相关在制文件：`CarriageEntityModel.java`、`CarriageItemModel.java`、`CarriageItemRenderer.java`、`CarriageItem.java`

- 完成 `CarriageWoodType` 及物品/NBT 对应逻辑，并补齐测试。
  - 计划中的相关文件：`src/main/java/com/monpai/sailboatmod/entity/CarriageWoodType.java`
  - 计划中的相关测试：`src/test/java/com/monpai/sailboatmod/entity/CarriageWoodTypeTest.java`

- 将马车运动逻辑彻底从“船逻辑兼容层”中拆出来，形成真正的陆地马车参数。
  - 重点包括：基础地面速度、路面加速、非道路减速、坡地阻力、驻车/靠站行为。
  - 相关在制文件：`CarriageEntity.java`

- 接入马车专用音效，而不是继续沿用船的音效语义。
  - 计划中的相关文件：`src/main/java/com/monpai/sailboatmod/registry/ModSounds.java`
  - 计划中的资源：`src/main/resources/assets/sailboatmod/sounds.json`

## 二、界面与工具显示修复

- 将“刷新/地图工具”按钮严格限制在圈地地图视图中显示，避免继续在 `nation`、`town` 的其他页面出现。
  - 计划中的相关文件：`ClaimsMapVisibility.java`
  - 相关在制文件：`NationHomeScreen.java`、`TownHomeScreen.java`

- 复核道路规划器物品模型与贴图资源，确保实际显示使用的是当前主线想要的资源。
  - 相关在制文件：`src/main/resources/assets/sailboatmod/models/item/road_planner.json`

## 三、驿站到驿站道路逻辑继续闭环

- 虽然主线已经加入 `PostStationRoadAnchorHelper`，但仍需要继续确认“道路真正连接到驿站等待区域”这一点是否在完整流程中全部生效。
  - 需要重点检查：`ManualRoadPlannerService` 中是否已经全面改为等待区域/出口驱动，而不是仍残留旧的边界锚点或回退逻辑。
  - 当前状态：辅助类已并入，完整业务闭环仍建议继续核查。

- 为驿站等待区道路连接补齐单测，而不是只停留在辅助类层面。
  - 建议补充：等待区出口选择、无可用出口回退、双城镇连接方向正确性。

## 四、道路预览与运行时表现继续打磨

- 继续复核幽灵方块/道路预览的固定坐标渲染体验。
  - 当前主线已做过修正，但仍建议继续检查玩家移动、斜坡段、长曲线段时的视觉稳定性。
  - 相关在制文件：`ConstructionGhostPreviewRenderer.java`、`RoadPlannerPreviewRenderer.java`

- 如果后续继续做道路施工进度可视化，需要把“谁能看到进度文本/预览”这套权限和距离规则统一下来。
  - 对应计划文件：`docs/superpowers/plans/2026-04-09-road-routing-and-runtime-progress.md`

## 五、道路寻路与地形处理后续项

- 继续加强 `RoadPathfinder` 与 `RoadRouteNodePlanner` 的“人类式选路”目标，而不只停留在当前已完成的贝塞尔平滑与加宽路面。
  - 包括：高坡惩罚、水域代价、绕建筑偏好、短桥优先而非长桥滥用。

- 继续完善连续坡段和阶梯式坡段的构造策略。
  - 当前主线已经完成 widened ribbon 和部分坡段支持，但仍需要继续验证极端高低差地形下是否存在断层、悬空、台阶感过强等问题。

- 继续评估道路照明与装饰的最终规则。
  - 当前主线已有 `RoadLightingPlanner` 辅助类。
  - 后续仍需要确认：间距、桥段是否禁灯、是否与道路地基/装饰冲突、是否需要更多测试。

## 六、仍未并回主线的在制改动来源

以下文件出现在当前备份 `stash@{0}` 中，说明这些方向曾经做到一半，但未正式并回 `main`：

- `.gitignore`
- `src/main/java/com/monpai/sailboatmod/SailboatMod.java`
- `src/main/java/com/monpai/sailboatmod/client/model/CarriageEntityModel.java`
- `src/main/java/com/monpai/sailboatmod/client/model/CarriageItemModel.java`
- `src/main/java/com/monpai/sailboatmod/client/renderer/CarriageItemRenderer.java`
- `src/main/java/com/monpai/sailboatmod/client/renderer/ConstructionGhostPreviewRenderer.java`
- `src/main/java/com/monpai/sailboatmod/client/renderer/RoadPlannerPreviewRenderer.java`
- `src/main/java/com/monpai/sailboatmod/client/screen/nation/NationHomeScreen.java`
- `src/main/java/com/monpai/sailboatmod/client/screen/town/TownHomeScreen.java`
- `src/main/java/com/monpai/sailboatmod/construction/RoadBezierCenterline.java`
- `src/main/java/com/monpai/sailboatmod/construction/RoadRouteNodePlanner.java`
- `src/main/java/com/monpai/sailboatmod/entity/CarriageEntity.java`
- `src/main/java/com/monpai/sailboatmod/item/CarriageItem.java`
- `src/main/resources/assets/sailboatmod/geo/carriage.geo.json`
- `src/main/resources/assets/sailboatmod/models/item/road_planner.json`
- `src/main/resources/assets/sailboatmod/textures/entity/carriage.png`

## 七、建议的下一步顺序

1. 先从 stash 中恢复马车系统相关改动到单独分支，优先完成马车木材、速度和音效闭环。
2. 然后处理 `ClaimsMapVisibility` 与 `nation/town` 页面上的按钮显示问题。
3. 接着复核驿站等待区到等待区的道路锚点完整链路，并补单测。
4. 最后再继续打磨道路预览稳定性、道路施工进度显示和道路照明规则。
