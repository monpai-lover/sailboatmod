# 未完成任务 1

## 说明

当前 `main` 已完成并发布的重点是道路规划器、贝塞尔道路 ribbon、道路生命周期、驿站道路锚点辅助与相关稳定性修复。  
本文件只记录截至 `2026-04-10` 仍未完成、未闭环，或者仍停留在本地备份中的内容。

当前还有一份未恢复的备份：

- `git stash show --stat "stash@{0}"`
- 说明：`backup-before-merge-feature-bezier-road-ribbon-into-main-2026-04-10`

这份 stash 主要包含马车系统、界面显示与部分道路预览相关的在制修改，尚未正式并入 `main`。

## 一、马车系统收尾（已在 `feature/carriage-wrap-up` worktree 闭环）

- 已完成：马车木材材质方案已落地，不再继续使用单一旧贴图。
  - 当前实现：按木材类型驱动模型贴图。
  - 相关实现：`CarriageEntityModel.java`、`CarriageItemModel.java`、`CarriageItemRenderer.java`、`CarriageItem.java`
  - 相关资源：`carriage_oak.png`、`carriage_spruce.png`、`carriage_dark_oak.png`

- 已完成：`CarriageWoodType` 及物品/NBT 对应逻辑已补齐，并已有自动化测试。
  - 相关实现：`src/main/java/com/monpai/sailboatmod/entity/CarriageWoodType.java`、`src/main/java/com/monpai/sailboatmod/item/CarriageItem.java`
  - 已覆盖：未知木材回退 oak、稳定循环顺序、stack NBT 读写、物品切换木材。
  - 相关测试：`src/test/java/com/monpai/sailboatmod/entity/CarriageWoodTypeTest.java`、`src/test/java/com/monpai/sailboatmod/item/CarriageItemTest.java`

- 已完成当前收口：马车运动逻辑已从船逻辑兼容层中抽出显式陆地参数，不再沿用单纯船只语义。
  - 当前规则：基础地面阻力、路面 bonus、非道路减速、坡地阻力、陆地驿站/靠站介质判断。
  - 相关实现：`CarriageEntity.java`
  - 相关测试：`CarriageEntityMovementTest.java`

- 已完成：马车专用音效已接入，不再继续沿用船的音效语义。
  - 当前实现：已注册 placement / attach / detach 相关音效资源，其中 attach / detach 已接入运行时 passenger transition。
  - 相关实现：`src/main/java/com/monpai/sailboatmod/registry/ModSounds.java`、`src/main/resources/assets/sailboatmod/sounds.json`
  - 相关测试：`CarriageEntitySoundCueTest.java`

## 二、界面与工具显示修复（已在 `feature/carriage-wrap-up` worktree 闭环）

- 已完成：将“刷新/地图工具”按钮严格限制在圈地地图视图中显示，不再在 `nation`、`town` 的其他页面泄漏。
  - 相关实现：`ClaimsMapVisibility.java`
  - 相关界面：`NationHomeScreen.java`、`TownHomeScreen.java`
  - 相关测试：`ClaimsMapVisibilityTest.java`

- 已复核：`src/main/resources/assets/sailboatmod/models/item/road_planner.json` 当前继续使用 `minecraft:item/compass` 作为显示资源；仓库内暂无新的 `road_planner` 专用物品贴图，因此这一项当前无额外代码变更需求。

## 三、驿站到驿站道路逻辑继续闭环（已在 `feature/carriage-wrap-up` worktree 闭环）

- 已完成：确认“道路真正连接到驿站等待区域”在手动规划主路径中生效，`ManualRoadPlannerService` 以等待区出口为主锚点路径，并修复了一个会在出口尚未解析前就提前判定失败的 strict-validation bug。
  - 相关实现：`ManualRoadPlannerService.java`
  - 相关辅助类：`PostStationRoadAnchorHelper.java`

- 已完成：为驿站等待区道路连接补齐单测，不再只停留在辅助类层面。
  - 已覆盖：等待区出口选择、无出口时回退、双向目标下的出口方向、strict failure 状态、等待区 footprint unblock 逻辑。
  - 相关测试：`PostStationRoadAnchorHelperTest.java`、`ManualRoadPlannerServiceTest.java`

## 四、道路预览与运行时表现继续打磨（已在 `feature/carriage-wrap-up` worktree 完成当前收口）

- 已完成：幽灵方块/道路预览的固定坐标渲染体验已按精确相机 `Vec3` 偏移收口，避免整数截断造成的抖动。
  - 相关实现：`ConstructionGhostPreviewRenderer.java`、`RoadPlannerPreviewRenderer.java`
  - 相关测试：`ConstructionGhostPreviewRendererTest.java`、`RoadPlannerPreviewRendererTest.java`

- 当前收口规则：预览/进度覆盖层仅在玩家手持道路规划器时显示，客户端 stale progress 会在超时后自动清空。
  - 相关实现：`RoadPlannerClientHooks.java`、`RoadPlannerPreviewRenderer.java`
  - 相关测试：`RoadPlannerClientHooksTest.java`

## 五、道路寻路与地形处理后续项（已在 `feature/carriage-wrap-up` worktree 完成当前收口）

- 已完成：`RoadPathfinder` 与 `RoadRouteNodePlanner` 当前的“人类式选路”规则已补成可验证闭环，不再停留在抽象目标。
  - 已覆盖：高坡惩罚、水域代价、terrain penalty、桥段预算与桥占比约束、优先走更平缓干燥的绕行路线。
  - 相关测试：`RoadRouteNodePlannerTest.java`

- 已完成当前收口：连续坡段/高低差地形仍沿用现有 widened ribbon、Bezier 与 slope 规划实现，但相关连续性与安全几何已由现有测试组覆盖。
  - 相关测试：`RoadGeometryPlannerSlopeTest.java`、`RoadGeometryPlannerRibbonTest.java`、`RoadBezierCenterlineTest.java`

- 已完成：道路照明规则已按当前实现收口。
  - 当前规则：固定间距、桥段禁灯、受保护列禁灯、灯柱偏移到行车带外侧。
  - 相关实现：`RoadLightingPlanner.java`
  - 相关测试：`RoadLightingPlannerTest.java`

## 六、仍未并回主线的在制改动来源

以下文件出现在当前备份 `stash@{0}` 中，说明这些方向曾经做到一半。当前 `feature/carriage-wrap-up` worktree 已把其中大部分方向做成可验证闭环，但尚未正式并回 `main`：

- `.gitignore`
- `src/main/java/com/monpai/sailboatmod/SailboatMod.java`
- `src/main/java/com/monpai/sailboatmod/client/model/CarriageEntityModel.java`
- `src/main/java/com/monpai/sailboatmod/client/model/CarriageItemModel.java`
- `src/main/java/com/monpai/sailboatmod/client/renderer/CarriageItemRenderer.java`
- `src/main/java/com/monpai/sailboatmod/client/renderer/ConstructionGhostPreviewRenderer.java`
- `src/main/java/com/monpai/sailboatmod/client/renderer/RoadPlannerPreviewRenderer.java`
- `src/main/java/com/monpai/sailboatmod/client/screen/nation/NationHomeScreen.java`
- `src/main/java/com/monpai/sailboatmod/client/screen/town/TownHomeScreen.java`
- `src/main/java/com/monpai/sailboatmod/construction/RoadBezierCenterline.java`
- `src/main/java/com/monpai/sailboatmod/construction/RoadRouteNodePlanner.java`
- `src/main/java/com/monpai/sailboatmod/entity/CarriageEntity.java`
- `src/main/java/com/monpai/sailboatmod/item/CarriageItem.java`
- `src/main/resources/assets/sailboatmod/geo/carriage.geo.json`
- `src/main/resources/assets/sailboatmod/models/item/road_planner.json`
- `src/main/resources/assets/sailboatmod/textures/entity/carriage.png`

## 七、当前状态

`未完成_任务_1.md` 记录的第 1 至第 5 节，当前都已在 `feature/carriage-wrap-up` worktree 中形成代码、测试与任务文件层面的闭环说明。

下一步不再是“继续补功能”，而是：

1. 评审当前 worktree 改动并整理提交边界。
2. 决定是分批提交 A/B/C/D，还是合并为一次任务 1 收口提交。
3. 再考虑如何把这批 worktree 改动并回主线。
